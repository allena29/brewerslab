



kickGui (i.e. close progress bar) should be triggered on findErrorPresneter.
 - stores doesn't seem to have a kickGui operation yet?
 
 
in recipe viewer move recalculate button to the top


if(tabletDevice){// && getWindowManager().getDefaultDisplay().getWidth() > getWindowManager().getDefaultDisplay().getHeight()){
window manager seems unavailable in common



json exception when decoding after creating a brewlog